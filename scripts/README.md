#!/bin/bash
# =============================================================================
# SGLang Orchestrator - Documentation & Reference
# =============================================================================

# --- MASTER DIRECTORY ---
# All primary configuration and logic resides in:
# ~/llms/sglang_orchestrator/scripts/

# --- CORE MODULES ---
# lib_params.sh        -> The single source of truth for model profiles & MTP.
# lib_docker.sh        -> Docker-based engine management.
# lib_venv.sh          -> Local VENV-based engine management.
# lib_api.sh           -> API exposure and bridging logic.
# intelligence.sh      -> HF download and system detection.
# operations.sh        -> System maintenance and utility functions.
# orchestrator.sh      -> The main interactive entry point.

# --- SPECLIGHT: Speculative Decoding (MTP) ---
# SGLang SpecBundle Implementation Pattern:
# 1. Profile defines: MTP_CAP (true/false), DRAFTER_REPO (HF path).
# 2. lib_docker.sh/lib_venv.sh checks MTP_CAP.
# 3. If true, injects:
#    --speculative-algorithm NEXTN
#    --speculative-draft-model-path [LOCAL_DRAFTER_PATH]
#    --speculative-num-steps 5
#    --speculative-num-draft-tokens 6

# --- MODEL UPDATE TRACKER ---
# Use the 'links.md' file to check for new HF repos or spec model updates.
# DO NOT edit this file manually; it is for reference.

# --- CURRENT VERSION ---
# Architecture: Modular Library-Driven (v1.0)
# Last Major Refactor: 2026-05-11 (Added lib_params.sh)
